# Synthetic BGM contract fixtures

These metadata-only fixtures exercise the BGM Core v1 contracts. They do not
contain, identify, or claim rights to real music. Asset byte counts and hashes
are deterministic placeholders; runtime integrity tests create disposable
synthetic files in temporary directories when actual bytes are required.

The fixture metadata is dedicated to the public domain under CC0-1.0.
