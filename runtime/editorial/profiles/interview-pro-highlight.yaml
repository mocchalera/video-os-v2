id: interview-pro-highlight
default_policy: interview
defaults:
  target_duration_sec: 90
  opening_cadence: "measured"
  middle_cadence: "deliberate"
  ending_cadence: "resolving"
  max_shot_length_frames: 192
  default_transition: "cut"
  crossfade_frames: 6
  active_editing_skills:
    - axis_hold_dialogue
    - b_roll_bridge
    - build_to_peak
    - reveal_then_payoff
    - silence_beat
    - shot_reverse_reaction
    - cooldown_resolve
    - exposition_release
  quality_target_overrides:
    hook_density_min: 0.4
    novelty_rate_min: 0.75
  trim_policy_overrides:
    mode: adaptive
    default_preferred_duration_frames: 96
    default_min_duration_frames: 36
    default_max_duration_frames: 192
