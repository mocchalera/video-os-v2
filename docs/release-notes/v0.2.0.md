# RoughCut Agent v0.2.0

Status: public repository snapshot updated on 2026-07-27. This version has not
been tagged as a GitHub release.

## Highlights

- Unified the video pipeline around shared phase execution, canonical artifact
  ownership, and one full-pipeline contract used by runtime entrypoints and
  Agent Skills.
- Added truthful analysis, planning, rendering, and freshness checks for audio,
  still images, and image sequences.
- Added Editorial Eye observation, relation-aware cut evaluation, benchmark
  contracts, and safer QA source discovery and mutation rollback.
- Expanded VideoOSStudio with caption and BGM review, editorial preference
  memory, render routing, package preflight, and end-to-end package contract
  verification.
- Hardened publication preflight, source-input attestation, package provenance,
  render-duration accounting, and stale-result protection.
- Added deterministic Remotion / HyperFrames routing, bundled Japanese fonts,
  and reusable social/interview finishing paths.
- Added final-render review packs, caption delivery and layout QA, speech
  cadence checks, clean-source attestations, and approval-bound YouTube upload
  receipts.

## Compatibility and behavior changes

- Node.js 22.x and npm 10.x remain required.
- The npm package remains `private`; v0.2.0 is a repository release, not an npm
  registry publication.
- Package success now requires current hashes and provenance for canonical
  timeline, QA, state, render inputs, and produced files. A worker exit code by
  itself is not sufficient.
- Canonical full-pipeline output includes `09_output/rough-cut.mp4`.
- Optional local-model paths remain fail-open, while release and package claims
  fail closed when required evidence is missing or stale.

## Public repository boundary

The published tree contains only `projects/_template/`, `projects/demo/`, and
`projects/sample/` under `projects/`. Source footage, generated renders,
checkout-local evaluation output, and private project artifacts are excluded.
`npm run verify:repo` enforces this allowlist.

## Release identity

- Previous public head: `84934cbaea867059a5448f7c3ddc4ebd5735f74d`
- Development source: `95c5c735eaf1f0a7b0a7b17adfcc5303774a550a`
- Candidate branch: `release/oss-v0.2.0`

## Release candidate verification

- Node.js `22.23.1`, npm `10.9.8`, and Apple Swift `6.2.4`.
- `npm run verify -- --full`: typecheck, demo schema, demo review metrics,
  golden-eval, and PARITY gates passed; PARITY mode passed 75 tests. The
  default parallel unit stage hit the 30-second timeout in one FFmpeg-backed
  review-pack test.
- `npx vitest run --maxWorkers=2`: 3,681 tests passed, 52 explicit skips, and
  no failures. The timed-out review-pack file also passed 3/3 when run alone.
- `swift test`: 602 tests passed with no failures.
- `npm --prefix editor run typecheck`: passed.
- `swift run videoos-studio-cli doctor`: passed against the two public example
  projects.
- Agent Skill, Studio cross-language fixture, agent-definition generation,
  repository hygiene, build, and demo schema checks passed.
- The OSS snapshot intentionally contains no approved private golden projects,
  so the full gate reported no golden projects to evaluate. Maintainers can add
  rights-cleared checkout-local goldens or pass `--projects` explicitly.
- Candidate-tree scans found no known secret-key pattern, developer-local user
  path, private development-repository URL, or excluded project ID.

## Publication gates

Before tagging or updating the public repository, a maintainer must confirm the
copyright holder in `LICENSE`, redistribution rights for `docs/images/`, and
the final public diff and history audit. Follow
[`docs/RELEASE_CHECKLIST.md`](../RELEASE_CHECKLIST.md).
